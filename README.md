# 📋 Todo Scheduler

Веб-приложение для управления задачами с повторяющимися событиями. Позволяет создавать, редактировать, удалять и отмечать выполненные задачи с поддержкой различных типов повторений.

**Ссылка на веб страницу:**
**https://todo.gsrcloud.ru/**
**Пароль: mysecretpassword123**

## ✅ Выполненные задания со звёздочкой

- ✅ **Аутентификация** - защита паролем через JWT токены                      
- ✅ **Docker-образ** - полная контейнеризация приложения
- ✅ **Поиск задач** - по заголовку, комментарию и дате
- ✅ **Полная реализация NextDate** - поддержка всех типов повторений (ежедневные, еженедельные, ежемесячные, ежегодные)

## 🗃️ Структура проекта

```
go_final_project/
├── main.go                    # Точка входа
├── .env                       # Переменные окружения
├── scripts/                   # Скрипты управления
│   ├── run.sh                 # Основной скрипт управления проектом
│   ├── backup.sh              # Резервное копирование базы данных
│   ├── restore.sh             # Восстановление базы данных
│   └── setup.sh               # Настройка проекта
├── pkg/                       # Backend
│   ├── api/                   # HTTP handlers и роутинг
│   │   ├── router.go          # Маршрутизация и handlers
│   │   ├── auth.go            # Аутентификация и middleware
│   │   ├── date_calculator.go # Расчет дат (NextDate, NormalizeDate)
│   │   └── response.go        # Форматирование ответов
│   ├── db/                    # Работа с базой данных
│   │   ├── db.go              # Основные операции с БД
│   │   ├── schema.sql         # Схема базы данных
│   └── model/                 # Структуры данных
│       ├── task.go            # Модель задачи
│       └── auth.go            # Модели аутентификации
├── tests/                     # Тесты
├── web/                       # Фронтенд (статичные файлы)
├── backups/                   # Бекап базы данных (создается при запуске backup.sh)
└── data/                      # Данные приложения (создается автоматически)
    └── scheduler.db           # База данных SQLite
```

## 🛠️ Технологический стек

### Backend
- **Go 1.25** - основной язык программирования
- **Chi Router** - HTTP роутер
- **JWT** - аутентификация с JSON Web Tokens
- **SQLite** - встраиваемая база данных
- **godotenv** - управление переменными окружения

### Frontend
- **HTML5/CSS3** - статический веб-интерфейс
- **Vanilla JavaScript** - клиентская логика без фреймворков

### Инфраструктура
- **Docker** - контейнеризация приложения
- **Docker Compose** - оркестрация контейнеров
- **Alpine Linux** - минималистичный базовый образ
- **Multi-stage builds** - оптимизация Docker образов

### Тестирование
- **Go Testing** - нативная система тестирования
- **Integration Tests** - полное тестирование API

### Dev Tools
- **Bash Scripts** - автоматизация управления
- **Health Checks** - мониторинг состояния
- **Backup/Restore** - системы резервного копирования

## 🏗️ Архитектура

```
Клиент ←→ HTTP API (Chi Router) ←→ Business Logic ←→ SQLite Database
     ↑          ↑                       ↑                 ↑
     │          │                       │                 │
   Frontend  JWT Auth             Date Calculator     SQLite Driver
```


## 🔧 API Endpoints

- `GET /` - Главная страница
- `GET /api/tasks` - Список задач
- `POST /api/task` - Создание задачи
- `PUT /api/task` - Редактирование задачи
- `DELETE /api/task` - Удаление задачи
- `POST /api/task/done` - Отметка выполнения
- `GET /api/nextdate` - Расчет следующей даты
- `POST /api/signin` - Аутентификация

## 🚀 Запуск проекта

### Автоматическая настройка (рекомендуется)

```bash
# Клонируйте репозиторий
git clone https://github.com/gsrlabs/go_final_project.git
cd go_final_project

# Запустите скрипт настройки
chmod +x scripts/setup.sh
./scripts/setup.sh
```

### Ручная настройка

1. **Создайте .env файл** в корне проекта:
```bash
cat > .env << EOF
TODO_PORT=7540
TODO_DBFILE=data/scheduler.db
TODO_PASSWORD=mysecretpassword123
EOF
```

## 🐳 Запуск через Docker

### Использование скриптов (рекомендуется)
```bash
# Просмотр всех доступных команд
./scripts/run.sh

# Запуск
./scripts/run.sh start

# Остановка
./scripts/run.sh stop

# Пересборка
./scripts/run.sh rebuild

# Просмотр логов
./scripts/run.sh logs
```

### Ручное управление Docker
```bash
# Сборка и запуск
docker compose up -d

# Остановка
docker compose down

# Пересборка
docker compose up --build -d
```
#### 💡 Важное примечание

При запуске проекта через Docker база данных создается с правами доступа, ограничивающими запись извне контейнера. Для корректной работы тестов необходимо предоставить дополнительные права:

```bash
sudo chmod -R 777 data/
```

### Запуск приложения

```bash
# Способ 1: Через скрипт
./scripts/run.sh dev

# Способ 2: Напрямую
go run main.go
```

### Доступ к приложению

Откройте в браузере: http://localhost:7540

## 🧪 Запуск тестов

### Полный набор тестов
```bash
./scripts/run.sh test
```
или
```bash
go test ./tests
```

### Получение токена для тестов
1. Запустите приложение: `./scripts/run.sh dev или ./scripts/run.sh start + ./scripts/run.sh logs`
2. Введите пароль: `mysecretpassword123`
3. Найдите в логах строку: `Token for testing:`
```bash
***
Token for testing:
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJwd2RfaGFzaCI6ImZlYjhhMjc0YzhiODk1MmNjN2YwYjZlMjYzMGJiMTkzNWU1YjlhNTM5NjA5YjUyZGQ3NTk3MzQ1ZTJjY2JiODkiLCJleHAiOjE3NjQzNjI2NDYsImlhdCI6MTc2NDMzMzg0Nn0.OCBXKRjGyWpQPIgi3O4x5azjjL0Q5ZolHmWwzC7HGtE
***
```
4. Скопируйте токен в `tests/settings.go`:
```go
var Port = 7540
var DBFile = "../data/scheduler.db"
var FullNextDate = true
var Search = true
var Token = `your_jwt_token_here`
```

## 📊 Скрипты управления

### Основные команды
```bash
./scripts/run.sh start          # Запуск
./scripts/run.sh stop           # Остановка
./scripts/run.sh restart        # Перезапуск
./scripts/run.sh rebuild        # Пересборка
./scripts/run.sh logs           # Логи в реальном времени
./scripts/run.sh status         # Статус контейнеров
./scripts/run.sh dev            # Локальный запуск (go run)
./scripts/run.sh test           # Запуск тестов
```

### Резервное копирование
```bash
# Создание бекапа (требует sudo)
sudo ./scripts/backup.sh

# Восстановление из бекапа
sudo ./scripts/restore.sh scheduler_20251128_191152.db

# Просмотр доступных бекапов
sudo ./scripts/restore.sh
```

### Очистка данных (ОСТОРОЖНО!)
```bash
./scripts/run.sh down-clean
```


## 🆘 Поиск проблем

### Приложение не запускается
- Проверьте что порт 7540 свободен
- Убедитесь что файл `.env` существует
- Проверьте логи: `./scripts/run.sh logs`

### Ошибки базы данных
- Убедитесь что папка `data/` существует
- Проверьте права доступа: `ls -la data/`
- Восстановите из бекапа при необходимости
- Откройте доступ к базе данных `sudo chmod -R 777 data/`

### Проблемы с аутентификацией
- Проверьте пароль в `.env` файле
- Убедитесь что JWT токен корректно сгенерирован
- Очистите cookies браузера

